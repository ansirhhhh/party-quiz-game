# 临时部署指南

## 🎉 最简单方案：使用 Cloudflare Tunnel（无需服务器）

### 步骤：
1. 安装 cloudflared：
   ```powershell
   npm install -g cloudflared
   ```

2. 确保开发服务器正在运行（在项目根目录）：
   ```powershell
   npm run dev
   ```

3. 新开一个终端，运行：
   ```powershell
   cloudflared tunnel --url http://localhost:3000
   ```

4. 你会获得一个临时链接，例如：`https://xxx.trycloudflare.com`，直接分享给朋友即可！

---

## 备选方案：使用 ngrok

### 步骤：
1. 去 [ngrok.com](https://ngrok.com/) 下载或使用 npm 安装：
   ```powershell
   npm install -g ngrok
   ```

2. 暴露本地端口：
   ```powershell
   ngrok http 3000
   ```

3. 获得临时链接分享。

---

## 如果你有云服务器：使用 Docker 部署

### 前置准备：
1. 已安装 Docker
2. 创建 `.env` 文件：
   ```bash
   cp .env.example .env
   ```
   （可选：填写 DATABASE_URL 等配置）

### 部署步骤：
```powershell
# 构建 Docker 镜像
docker build -t quiz-app .

# 运行容器
docker run -d \
  --name quiz-app \
  -p 3000:3000 \
  --env-file .env \
  --restart unless-stopped \
  quiz-app
```

---

## 使用 Railway 等 PaaS 平台

1. 将项目推送到 GitHub
2. 在 [Railway](https://railway.app/) 或 [Render](https://render.com/) 上连接仓库
3. 自动部署即可

---

## 📝 注意事项

- 临时链接（Cloudflare Tunnel/ngrok）只在你保持终端运行时有效
- 如果需要长期部署，建议使用 Docker 或 PaaS 平台
- WebSocket 功能在这些方案中都可以正常使用
